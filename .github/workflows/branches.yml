name: build_branch
on:
  push:
    branches-ignore:
      - master
    #paths:
    #  - ".workflows/**/*.*" 
    #  - "src/**/*.*"
      
env:
  prerelease: -beta
jobs:
  build_branch:
    name: build_branch
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # Get latest tag
      # perform semver coerce
      # increment patch
      # create package_version that adds optional prerelease
      - id: compute_version
        name: Compute version
        uses: craig-day/compute-tag@v10
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          version_type: patch          
      - id: compute_package_version
        name: Compute package version
        run: echo "::set-output name=package_version::${{ steps.compute_version.outputs.next_tag }}${{ env.prerelease }}"
          
      
      - name: Get version
        run: echo "The version is ${{ steps.compute_package_version.outputs.package_version }}"

      #- name: setup_dotnet
      #  uses: actions/setup-dotnet@v1
      #  with:
      #    dotnet-version: 3.1.200      
      #- name: set_dotnet_roll_forward
      #  uses: allenevans/set-env@v1.0.0
      #  with:
      #    DOTNET_ROLL_FORWARD_ON_NO_CANDIDATE_FX: '2'
      #    
      #- name: install_dependencies
      #  run: dotnet restore ./src/Solid.Identity.Protocols.Saml2p.sln
      #- name: build
      #  run: dotnet build ./src/Solid.Identity.Protocols.Saml2p.sln --configuration Release --no-restore -p:VersionPrefix=${{ env.version }}
      #- name: test
      #  run: dotnet test ./src/Solid.Identity.Protocols.Saml2p.sln --configuration Release --no-restore --no-build
